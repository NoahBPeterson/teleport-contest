// Generated by tools/c2js — do not edit by hand
// Input: nethack-c/recorder/src/rumors.c
// Input sha256: 0bc8665c1bcff5b2d9d9063ff02dfd081ddcb01ba0ba5c817f0dbf857d73c255
// Transpiler: tools/c2js c2js emit v1+batch

import { schar } from '../cmachine.js';
import * as cptr from '../cptr.js';
import { eos, lowc, xcrypt } from './hacklib.js';
import { WIN_MESSAGE, disp, flags, gf, gi, gm, go, gt, iflags, program_state, svo, u, ynchars, ynqchars } from './decl.js';
import { rn2, rnd, rng_log_enabled, rng_log_set_caller } from './rnd.js';
import { There, You, impossible, nhassert_failed, pline, verbalize } from './pline.js';
import { exercise } from './attrib.js';
import { windowprocs } from './windows.js';
import { alloc, dupstr } from './alloc.js';
import { is_fainted } from './eat.js';
import { money_cnt } from './hack.js';
import { Monnam, bogon_is_pname } from './do_name.js';
import { currency } from './invent.js';
import { yn_function } from './cmd.js';
import { money2mon } from './shk.js';
import { record_achievement } from './insight.js';
import { more_experienced, newexplevel } from './exper.js';
import { mons } from './monst.js';
import { the_unique_pm } from './objnam.js';
import { debugcore } from './files.js';

// string literals (C char* uses decay to CPtr into these static buffers)
const __sl0 = cptr.lit("rumors");
const __sl1 = cptr.lit("r");
const __sl2 = cptr.lit("Error reading \"%.80s\".");
const __sl3 = cptr.lit("/Users/noahpeterson/Documents/Projects/teleport-contest-research/original-contest-to-fork/nethack-c/recorder/src/rumors.c");
const __sl4 = cptr.lit("getrumor");
const __sl5 = cptr.lit("strange truth value for rumor");
const __sl6 = cptr.lit("Oops...");
const __sl7 = cptr.lit("Can't find non-cookie rumor?");
const __sl8 = cptr.lit("[cookie] ");
const __sl9 = cptr.lit("T start=%06ld (%06lx), end=%06ld (%06lx), size=%06ld (%06lx)");
const __sl10 = cptr.lit("F start=%06ld (%06lx), end=%06ld (%06lx), size=%06ld (%06lx)");
const __sl11 = cptr.lit("T %06ld %s");
const __sl12 = cptr.lit("  %6s %s");
const __sl13 = cptr.lit("");
const __sl14 = cptr.lit("F %06ld %s");
const __sl15 = cptr.lit("rumors not accessible.");
const __sl16 = cptr.lit("Engravings:");
const __sl17 = cptr.lit("engrave");
const __sl18 = cptr.lit("Epitaphs:");
const __sl19 = cptr.lit("epitaph");
const __sl20 = cptr.lit("Bogus monsters:");
const __sl21 = cptr.lit("bogusmon");
const __sl22 = cptr.lit("can't create temporary window");
const __sl23 = cptr.lit("error; can't read comment line");
const __sl24 = cptr.lit("malformed; first line is not a comment line:");
const __sl25 = cptr.lit("- first line, as is");
const __sl26 = cptr.lit("- xcrypt of first line");
const __sl27 = cptr.lit("can't read first non-comment line");
const __sl28 = cptr.lit("first non-comment line is empty");
const __sl29 = cptr.lit("(no second entry)");
const __sl30 = cptr.lit("(only two entries)");
const __sl31 = cptr.lit(" ...");
const __sl32 = cptr.lit("filechunksize <= INT_MAX");
const __sl33 = cptr.lit("What a pity that you cannot read it!");
const __sl34 = cptr.lit("NetHack rumors file closed for renovation.");
const __sl35 = cptr.lit("True to her word, the Oracle %ssays: ");
const __sl36 = cptr.lit("outrumor");
const __sl37 = cptr.lit("offhandedly ");
const __sl38 = cptr.lit("casually ");
const __sl39 = cptr.lit("nonchalantly ");
const __sl40 = cptr.lit("%s");
const __sl41 = cptr.lit("It reads:");
const __sl42 = cptr.lit("%5d\n");
const __sl43 = cptr.lit("%5lx\n");
const __sl44 = cptr.lit("oracle-oracle_cnt");
const __sl45 = cptr.lit("oracle-oracle_loc");
const __sl46 = cptr.lit("oracles");
const __sl47 = cptr.lit("outoracle");
const __sl48 = cptr.lit("The Oracle scornfully takes all your gold and says:");
const __sl49 = cptr.lit("The Oracle meditates for a moment and then intones:");
const __sl50 = cptr.lit("The message reads:");
const __sl51 = cptr.lit("---\n");
const __sl52 = cptr.lit("is no one here to consult.");
const __sl53 = cptr.lit("%s is in no mood for consultations.");
const __sl54 = cptr.lit("have no gold.");
const __sl55 = cptr.lit("\"Wilt thou settle for a minor consultation?\" (%d %s)");
const __sl56 = cptr.lit("don't even have enough gold for that!");
const __sl57 = cptr.lit("\"Then dost thou desire a major one?\" (%d %s)");
const __sl58 = cptr.lit("Can't open '%s' file.");
const __sl59 = cptr.lit("CapitalMon");
const __sl60 = cptr.lit("rumors.c");
const __sl61 = cptr.lit("CapMons != 0");
const __sl62 = cptr.lit("CapMons");
const __sl63 = cptr.lit("Capitalized monster type names normally preceded by \"the\":");
const __sl64 = cptr.lit("  %.77s");

/** C ref: rumors.c:56 — unsigned int */
let CapMonstCnt = 0;

/** C ref: rumors.c:56 — unsigned int */
let CapBogonCnt = 0;

/** C ref: rumors.c:57 — unsigned int */
let CapMonSiz = 0;

/** C ref: rumors.c:58 — char ** */
let CapMons = null;

/** C ref: rumors.c:67 — @param {CPtr} line */
function unpadline(line) {
    let p = eos.v(line);
    if (cptr.cmp(p, line) > 0 && cptr.ld1s(cptr.add(p, -1)) == 10)
        p = cptr.add(p, -1);
    while (cptr.cmp(p, line) > 0 && cptr.ld1s(cptr.add(p, -1)) == 95)
        p = cptr.add(p, -1);
    cptr.st1(p, 0);
}

const __static_init_rumors_rumors_header = cptr.bytes("%d,%ld,%lx;%d,%ld,%lx;0,0,%lx\n"); /** C ref: rumors.c:87 — char[31] (function-static) */

/** C ref: rumors.c:85 — @param {CPtr} fp */
function init_rumors(fp) {
    let true_count = cptr.box(0);
    let false_count = cptr.box(0);
    let eof_offset = cptr.box(0);
    let line = new Uint8Array(256);
    void fgets(cptr.decay(line), 256, fp);
    void fgets(cptr.decay(line), 256, fp);
    if (sscanf(cptr.decay(line), cptr.decay(__static_init_rumors_rumors_header), true_count, cptr.boxProp(gt, 'true_rumor_size'), cptr.boxProp(gt, 'true_rumor_start'), false_count, cptr.boxProp(gf, 'false_rumor_size'), cptr.boxProp(gf, 'false_rumor_start'), eof_offset) == 7 && gt.true_rumor_size > 0n && gf.false_rumor_size > 0n) {
        gt.true_rumor_end = BigInt.asIntN(64, gt.true_rumor_start) + gt.true_rumor_size;
        gf.false_rumor_end = BigInt.asIntN(64, gf.false_rumor_start) + gf.false_rumor_size;
    } else {
        gt.true_rumor_size = -1n;
        void fclose(fp);
    }
}

let __static_getrumor_cookie_marker = __sl8; /** C ref: rumors.c:125 — char * (function-static) */

/** C ref: rumors.c:117 — @param {CInt} truth @param {CPtr} rumor_buf @param {CInt} exclude_cookie @returns {CPtr} */
export function getrumor(truth, rumor_buf, exclude_cookie) {
    let rumors;
    let beginning;
    let ending;
    let line = new Uint8Array(256);
    let marklen = Number(BigInt.asIntN(32, cptr.strlen(__static_getrumor_cookie_marker)));
    cptr.st1(cptr.add(rumor_buf, 0), 0);
    if (gt.true_rumor_size < 0n)
        return rumor_buf;
    rumors = fopen(__sl0, __sl1);
    if (rumors) {
        let count = 0;
        let adjtruth;
        do {
            cptr.st1(cptr.add(rumor_buf, 0), 0);
            if (gt.true_rumor_size == 0n) {
                init_rumors(rumors);
                if (gt.true_rumor_size < 0n) {
                    void cptr.sprintf(rumor_buf, __sl2, __sl0);
                    return rumor_buf;
                }
            }
            switch (adjtruth = (truth + (rng_log_enabled() ? (rng_log_set_caller(__sl3, 151, __sl4), rn2(2)) : rn2(2))) | 0) {
                case 2:
                case 1:
                beginning = BigInt.asIntN(64, gt.true_rumor_start);
                ending = gt.true_rumor_end;
                break;
                case 0:
                case -1:
                beginning = BigInt.asIntN(64, gf.false_rumor_start);
                ending = gf.false_rumor_end;
                break;
                default:
                impossible(__sl5);
                return cptr.strcpy(rumor_buf, __sl6);
            }
            void cptr.strcpy(rumor_buf, get_rnd_line(rumors, cptr.decay(line), 256, rn2, beginning, ending, 60));
        } while (count++ < 50 && exclude_cookie && !cptr.strncmp(rumor_buf, __static_getrumor_cookie_marker, BigInt.asUintN(64, BigInt(marklen))));
        void fclose(rumors);
        if (count >= 50)
            impossible(__sl7);
        else if (!gi.in_mklev)
            exercise(A_WIS, schar((adjtruth > 0)));
    } else {
        couldnt_open_file(__sl0);
        gt.true_rumor_size = BigInt((-1));
    }
    if (!exclude_cookie && !cptr.strncmp(rumor_buf, __static_getrumor_cookie_marker, BigInt.asUintN(64, BigInt(marklen)))) {
        let src = cptr.add(rumor_buf, marklen);
        let dst = rumor_buf;
        for (; cptr.ld1s(src) != 0; src = cptr.add(src, 1), dst = cptr.add(dst, 1)) {
            cptr.st1(dst, cptr.ld1s(src));
        }
        cptr.st1(dst, 0);
    }
    return rumor_buf;
}

/** C ref: rumors.c:196 */
export function rumor_check() {
    let __go_no_rumors = false;
    __skip_no_rumors: {
        let rumors;
        let tmpwin = cptr.box((-1));
        let endp;
        let line = new Uint8Array(256);
        let xbuf = new Uint8Array(256);
        let rumor_buf = new Uint8Array(256);
        rumors = (gt.true_rumor_size >= 0n) ? fopen(__sl0, __sl1) : null;
        if (rumors) {
            let ftell_rumor_start = 0n;
            cptr.st1(cptr.add(cptr.decay(rumor_buf), 0), 0);
            if (gt.true_rumor_size == 0n) {
                init_rumors(rumors);
                if (gt.true_rumor_size < 0n) {
                    rumors = null;
                    { __go_no_rumors = true; break __skip_no_rumors; }
                }
            }
            tmpwin.v = (windowprocs.win_create_nhwindow)(5);
            void cptr.sprintf(cptr.decay(rumor_buf), __sl9, BigInt.asIntN(64, gt.true_rumor_start), gt.true_rumor_start, gt.true_rumor_end, BigInt.asUintN(64, gt.true_rumor_end), gt.true_rumor_size, BigInt.asUintN(64, gt.true_rumor_size));
            (windowprocs.win_putstr)(tmpwin.v, 0, cptr.decay(rumor_buf));
            void cptr.sprintf(cptr.decay(rumor_buf), __sl10, BigInt.asIntN(64, gf.false_rumor_start), gf.false_rumor_start, gf.false_rumor_end, BigInt.asUintN(64, gf.false_rumor_end), gf.false_rumor_size, BigInt.asUintN(64, gf.false_rumor_size));
            (windowprocs.win_putstr)(tmpwin.v, 0, cptr.decay(rumor_buf));
            cptr.st1(cptr.add(cptr.decay(rumor_buf), 0), 0);
            void fseek(rumors, BigInt.asIntN(64, gt.true_rumor_start), 0);
            ftell_rumor_start = ftell(rumors);
            void fgets(cptr.decay(line), 256, rumors);
            if ((endp = cptr.strchr(cptr.decay(line), 10)) !== null)
                cptr.st1(endp, 0);
            void cptr.sprintf(cptr.decay(rumor_buf), __sl11, ftell_rumor_start, xcrypt(cptr.decay(line), cptr.decay(xbuf)));
            (windowprocs.win_putstr)(tmpwin.v, 0, cptr.decay(rumor_buf));
            while (fgets(cptr.decay(line), 256, rumors) && ftell(rumors) < gt.true_rumor_end)
                continue;
            if ((endp = cptr.strchr(cptr.decay(line), 10)) !== null)
                cptr.st1(endp, 0);
            void cptr.sprintf(cptr.decay(rumor_buf), __sl12, __sl13, xcrypt(cptr.decay(line), cptr.decay(xbuf)));
            (windowprocs.win_putstr)(tmpwin.v, 0, cptr.decay(rumor_buf));
            cptr.st1(cptr.add(cptr.decay(rumor_buf), 0), 0);
            void fseek(rumors, BigInt.asIntN(64, gf.false_rumor_start), 0);
            ftell_rumor_start = ftell(rumors);
            void fgets(cptr.decay(line), 256, rumors);
            if ((endp = cptr.strchr(cptr.decay(line), 10)) !== null)
                cptr.st1(endp, 0);
            void cptr.sprintf(cptr.decay(rumor_buf), __sl14, ftell_rumor_start, xcrypt(cptr.decay(line), cptr.decay(xbuf)));
            (windowprocs.win_putstr)(tmpwin.v, 0, cptr.decay(rumor_buf));
            while (fgets(cptr.decay(line), 256, rumors) && ftell(rumors) < gf.false_rumor_end)
                continue;
            if ((endp = cptr.strchr(cptr.decay(line), 10)) !== null)
                cptr.st1(endp, 0);
            void cptr.sprintf(cptr.decay(rumor_buf), __sl12, __sl13, xcrypt(cptr.decay(line), cptr.decay(xbuf)));
            (windowprocs.win_putstr)(tmpwin.v, 0, cptr.decay(rumor_buf));
            void fclose(rumors);
        } else if (gt.true_rumor_size < 0n) {
            __go_no_rumors = true; break __skip_no_rumors;
        } else {
            couldnt_open_file(__sl0);
            gt.true_rumor_size = BigInt((-1));
        }
    }
    if (__go_no_rumors) {
        pline(__sl15);
        (windowprocs.win_display_nhwindow)(WIN_MESSAGE, (1));
    }
    others_check(__sl16, __sl17, tmpwin);
    others_check(__sl18, __sl19, tmpwin);
    others_check(__sl20, __sl21, tmpwin);
    if (tmpwin.v != (-1)) {
        (windowprocs.win_display_nhwindow)(tmpwin.v, (1));
        (windowprocs.win_destroy_nhwindow)(tmpwin.v);
    }
}

const __static_others_check_errfmt = cptr.bytes("others_check(\"%s\"): %s"); /** C ref: rumors.c:313 — char[23] (function-static) */

/** C ref: rumors.c:308 — @param {CPtr} ftype @param {CPtr} fname @param {CPtr} winptr */
function others_check(ftype, fname, winptr) {
    let fh;
    let line = new Uint8Array(256);
    let xbuf = new Uint8Array(256);
    let endp;
    let tmpwin = cptr.ldI32(winptr);
    let entrycount = 0;
    fh = fopen(fname, __sl1);
    if (fh) {
        __lbl_closeit: {
            if (tmpwin == (-1)) {
                cptr.stI32(winptr, tmpwin = (windowprocs.win_create_nhwindow)(5));
                if (tmpwin == (-1)) {
                    impossible(cptr.decay(__static_others_check_errfmt), fname, __sl22);
                    break __lbl_closeit;
                }
            }
            (windowprocs.win_putstr)(tmpwin, 0, __sl13);
            (windowprocs.win_putstr)(tmpwin, 0, ftype);
            cptr.st1(cptr.decay(line), 0);
            if (!fgets(cptr.decay(line), 256, fh)) {
                void cptr.sprintf(cptr.decay(xbuf), cptr.decay(__static_others_check_errfmt), fname, __sl23);
                (windowprocs.win_putstr)(tmpwin, 0, cptr.decay(xbuf));
                break __lbl_closeit;
            }
            if (cptr.ld1s(cptr.decay(line)) != 35) {
                void cptr.sprintf(cptr.decay(xbuf), cptr.decay(__static_others_check_errfmt), fname, __sl24);
                (windowprocs.win_putstr)(tmpwin, 0, cptr.decay(xbuf));
                if ((endp = cptr.strchr(cptr.decay(line), 10)) !== null)
                    cptr.st1(endp, 0);
                (windowprocs.win_putstr)(tmpwin, 0, __sl25);
                (windowprocs.win_putstr)(tmpwin, 0, cptr.decay(line));
                (windowprocs.win_putstr)(tmpwin, 0, __sl26);
                (windowprocs.win_putstr)(tmpwin, 0, xcrypt(cptr.decay(line), cptr.decay(xbuf)));
                break __lbl_closeit;
            }
            cptr.st1(cptr.decay(line), 0);
            if (!fgets(cptr.decay(line), 256, fh) || cptr.ld1s(cptr.decay(line)) == 10) {
                void cptr.sprintf(cptr.decay(xbuf), cptr.decay(__static_others_check_errfmt), fname, !cptr.ld1s(cptr.decay(line)) ? __sl27 : __sl28);
                (windowprocs.win_putstr)(tmpwin, 0, cptr.decay(xbuf));
                break __lbl_closeit;
            }
            ++entrycount;
            if ((endp = cptr.strchr(cptr.decay(line), 10)) !== null)
                cptr.st1(endp, 0);
            (windowprocs.win_putstr)(tmpwin, 0, xcrypt(cptr.decay(line), cptr.decay(xbuf)));
            if (!fgets(cptr.decay(line), 256, fh)) {
                (windowprocs.win_putstr)(tmpwin, 0, __sl29);
            } else {
                ++entrycount;
                if ((endp = cptr.strchr(cptr.decay(line), 10)) !== null)
                    cptr.st1(endp, 0);
                (windowprocs.win_putstr)(tmpwin, 0, xcrypt(cptr.decay(line), cptr.decay(xbuf)));
                while (fgets(cptr.decay(line), 256, fh)) {
                    ++entrycount;
                    if ((endp = cptr.strchr(cptr.decay(line), 10)) !== null)
                        cptr.st1(endp, 0);
                    void xcrypt(cptr.decay(line), cptr.decay(xbuf));
                }
                if (entrycount == 2) {
                    (windowprocs.win_putstr)(tmpwin, 0, __sl30);
                } else {
                    if (entrycount > 3)
                        (windowprocs.win_putstr)(tmpwin, 0, __sl31);
                    (windowprocs.win_putstr)(tmpwin, 0, cptr.decay(xbuf));
                }
            }
        }
        void fclose(fh);
    } else {
        couldnt_open_file(fname);
    }
}

/** C ref: rumors.c:420 — @param {CPtr} fh @param {CPtr} buf @param {CUInt} bufsiz @param {CPtr} rng @param {CLongLong} startpos @param {CLongLong} endpos @param {CUInt} padlength @returns {CPtr} */
function get_rnd_line(fh, buf, bufsiz, rng, startpos, endpos, padlength) {
    let newl;
    let xbufp;
    let xbuf = new Uint8Array(256);
    let filechunksize;
    let chunkoffset;
    let trylimit;
    cptr.st1(buf, 0);
    if (!endpos) {
        void fseek(fh, 0n, 2);
        endpos = ftell(fh);
    }
    filechunksize = endpos - startpos;
    if (filechunksize < 1n)
        return buf;
    void ((!!(filechunksize <= 2147483647n)) || (nhassert_failed(__sl32, __sl3, 449), 0));
    for (trylimit = 10; trylimit > 0; --trylimit) {
        chunkoffset = BigInt((rng)(Number(BigInt.asIntN(32, filechunksize))));
        void fseek(fh, startpos + chunkoffset, 0);
        void fgets(buf, bufsiz | 0, fh);
        if (!padlength || Number(BigInt.asUintN(32, cptr.strlen(buf))) <= (padlength + 1) >>> 0)
            break;
    }
    if (ftell(fh) >= endpos || !fgets(buf, bufsiz | 0, fh)) {
        void fseek(fh, startpos, 0);
        void fgets(buf, bufsiz | 0, fh);
    }
    if ((newl = cptr.strchr(buf, 10)) !== null)
        cptr.st1(newl, 0);
    xbufp = (cptr.strlen(buf) <= 256n - 1n) ? cptr.add(cptr.decay(xbuf), 0) : alloc((Number(BigInt.asUintN(32, cptr.strlen(buf))) + 1) >>> 0);
    void cptr.strcpy(buf, xcrypt(buf, xbufp));
    if (!cptr.eq(xbufp, cptr.add(cptr.decay(xbuf), 0)))
        cptr.free(xbufp);
    if (padlength)
        unpadline(buf);
    return buf;
}

/** C ref: rumors.c:499 — @param {CPtr} fname @param {CPtr} buf @param {CPtr} rng @param {CUInt} padlength @returns {CPtr} */
export function get_rnd_text(fname, buf, rng, padlength) {
    let fh = fopen(fname, __sl1);
    cptr.st1(cptr.add(buf, 0), 0);
    if (fh) {
        let starttxt = 0n;
        let line = new Uint8Array(256);
        void fgets(cptr.decay(line), 256, fh);
        void fseek(fh, 0n, 1);
        starttxt = ftell(fh);
        void cptr.strcpy(buf, get_rnd_line(fh, cptr.decay(line), 256, rng, starttxt, 0n, padlength));
        void fclose(fh);
    } else {
        couldnt_open_file(fname);
    }
    return buf;
}

const __static_outrumor_fortune_msg = cptr.bytes("This cookie has a scrap of paper inside."); /** C ref: rumors.c:533 — char[41] (function-static) */

/** C ref: rumors.c:529 — @param {CInt} truth @param {CInt} mechanism */
export function outrumor(truth, mechanism) {
    let line;
    let buf = new Uint8Array(256);
    let reading = schar((mechanism == 1 || mechanism == 2));
    if (reading) {
        if (is_fainted() && mechanism == 1) {
            return;
        } else if (((u.uprops[BLINDED].intrinsic || u.uprops[BLINDED].extrinsic) && !u.uprops[BLINDED].blocked)) {
            if (mechanism == 1)
                pline(cptr.decay(__static_outrumor_fortune_msg));
            pline(__sl33);
            return;
        }
    }
    line = getrumor(truth, cptr.decay(buf), schar((reading ? 0 : 1)));
    if (!cptr.ld1s(line))
        line = __sl34;
    switch (mechanism) {
        case 0:
        pline(__sl35, (!(rng_log_enabled() ? (rng_log_set_caller(__sl3, 558, __sl36), rn2(4)) : rn2(4)) ? __sl37 : (!(rng_log_enabled() ? (rng_log_set_caller(__sl3, 559, __sl36), rn2(3)) : rn2(3)) ? __sl38 : ((rng_log_enabled() ? (rng_log_set_caller(__sl3, 560, __sl36), rn2(2)) : rn2(2)) ? __sl39 : __sl13))));
        ;
        verbalize(__sl40, line);
        return;
        case 1:
        pline(cptr.decay(__static_outrumor_fortune_msg));
        // @FallThrough
        ;
        case 2:
        pline(__sl41);
        break;
    }
    pline(__sl40, line);
}

/** C ref: rumors.c:577 — @param {CPtr} fp */
function init_oracles(fp) {
    let i;
    let line = new Uint8Array(256);
    let cnt = cptr.box(0);
    void fgets(cptr.decay(line), 256, fp);
    void fgets(cptr.decay(line), 256, fp);
    if (sscanf(cptr.decay(line), __sl42, cnt) == 1 && cnt.v > 0) {
        svo.oracle_cnt = cnt.v >>> 0;
        svo.oracle_loc = alloc(Number(BigInt.asUintN(32, (BigInt((cnt.v >>> 0) >>> 0) * 8n))));
        for (i = 0; i < cnt.v; i++) {
            void fgets(cptr.decay(line), 256, fp);
            void sscanf(cptr.decay(line), __sl43, cptr.add(svo.oracle_loc, i));
        }
    }
    return;
}

/** C ref: rumors.c:598 — @param {CPtr} nhfp */
export function save_oracles(nhfp) {
    let i;
    if ((cptr.ldI32(cptr.add((nhfp), 4)) & (1 | 2))) {
        sfo_unsigned(nhfp, cptr.boxProp(svo, 'oracle_cnt'), __sl44);
        if (svo.oracle_cnt) {
            for (i = 0; i >>> 0 < svo.oracle_cnt; ++i) {
                sfo_ulong(nhfp, cptr.add(svo.oracle_loc, i), __sl45);
                ;
            }
        }
    }
    if ((cptr.ldI32(cptr.add((nhfp), 4)) & 4)) {
        if (svo.oracle_cnt) {
            svo.oracle_cnt = 0, go.oracle_flg = 0;
        }
        if (svo.oracle_loc) {
            cptr.free(svo.oracle_loc);
            svo.oracle_loc = null;
        }
    }
}

/** C ref: rumors.c:623 — @param {CPtr} nhfp */
export function restore_oracles(nhfp) {
    let i;
    sfi_unsigned(nhfp, cptr.boxProp(svo, 'oracle_cnt'), __sl44);
    ;
    if (svo.oracle_cnt) {
        svo.oracle_loc = alloc(Number(BigInt.asUintN(32, (BigInt(svo.oracle_cnt >>> 0) * 8n))));
        for (i = 0; i >>> 0 < svo.oracle_cnt; ++i) {
            sfi_ulong(nhfp, cptr.add(svo.oracle_loc, i), __sl45);
            ;
        }
        go.oracle_flg = 1;
    }
}

/** C ref: rumors.c:640 — @param {CInt} special @param {CInt} delphi */
export function outoracle(special, delphi) {
    let tmpwin;
    let oracles;
    let oracle_idx;
    let endp;
    let line = new Uint8Array(80);
    let xbuf = new Uint8Array(256);
    if (go.oracle_flg < 0 || (go.oracle_flg > 0 && svo.oracle_cnt == 0))
        return;
    oracles = fopen(__sl46, __sl1);
    if (oracles) {
        __lbl_close_oracles: {
            if (go.oracle_flg == 0) {
                init_oracles(oracles);
                go.oracle_flg = 1;
                if (svo.oracle_cnt == 0)
                    break __lbl_close_oracles;
            }
            if (svo.oracle_cnt <= 1 && !special)
                break __lbl_close_oracles;
            oracle_idx = special ? 0 : (rng_log_enabled() ? (rng_log_set_caller(__sl3, 665, __sl47), rnd(((svo.oracle_cnt | 0) - 1) | 0)) : rnd(((svo.oracle_cnt | 0) - 1) | 0));
            void fseek(oracles, BigInt.asIntN(64, cptr.ldU64(cptr.add(svo.oracle_loc, oracle_idx))), 0);
            if (!special)
                cptr.stU64(cptr.add(svo.oracle_loc, oracle_idx), cptr.ldU64(cptr.add(svo.oracle_loc, --svo.oracle_cnt)));
            tmpwin = (windowprocs.win_create_nhwindow)(5);
            if (delphi)
                (windowprocs.win_putstr)(tmpwin, 0, special ? __sl48 : __sl49);
            else
                (windowprocs.win_putstr)(tmpwin, 0, __sl50);
            (windowprocs.win_putstr)(tmpwin, 0, __sl13);
            while (fgets(cptr.decay(line), 80, oracles) && strcmp(cptr.decay(line), __sl51)) {
                if ((endp = cptr.strchr(cptr.decay(line), 10)) !== null)
                    cptr.st1(endp, 0);
                (windowprocs.win_putstr)(tmpwin, 0, xcrypt(cptr.decay(line), cptr.decay(xbuf)));
            }
            (windowprocs.win_display_nhwindow)(tmpwin, (1));
            (windowprocs.win_destroy_nhwindow)(tmpwin);
        }
        void fclose(oracles);
    } else {
        couldnt_open_file(__sl46);
        go.oracle_flg = -1;
    }
}

/** C ref: rumors.c:696 — @param {CPtr} oracl @returns {CInt} */
export function doconsult(oracl) {
    let umoney;
    let u_pay;
    let minor_cost = 50;
    let major_cost = (500 + Math.imul(50, u.ulevel)) | 0;
    let add_xpts;
    let qbuf = new Uint8Array(128);
    gm.multi = 0n;
    umoney = money_cnt(gi.invent);
    if (!oracl) {
        There(__sl52);
        return 0;
    } else if (!cptr.ldI32(cptr.add(oracl, 168))) {
        pline(__sl53, Monnam(oracl));
        return 0;
    } else if (!umoney) {
        You(__sl54);
        return 0;
    }
    void cptr.sprintf(cptr.decay(qbuf), __sl55, minor_cost, currency(BigInt(minor_cost)));
    switch (yn_function(cptr.decay(qbuf), cptr.decay(ynqchars), 113, (1))) {
        default:
        case 113:
        return 0;
        case 121:
        if (umoney < BigInt(minor_cost)) {
            You(__sl56);
            return 0;
        }
        u_pay = minor_cost;
        break;
        case 110:
        if (umoney <= BigInt(minor_cost) || (svo.oracle_cnt == 1 || go.oracle_flg < 0))
            return 0;
        void cptr.sprintf(cptr.decay(qbuf), __sl57, major_cost, currency(BigInt(major_cost)));
        if (yn_function(cptr.decay(qbuf), cptr.decay(ynchars), 110, (1)) != 121)
            return 0;
        u_pay = (umoney < BigInt(major_cost)) ? Number(BigInt.asIntN(32, umoney)) : major_cost;
        break;
    }
    money2mon(oracl, BigInt(u_pay));
    disp.botl = (1);
    if (!u.uevent.major_oracle && !u.uevent.minor_oracle)
        record_achievement(schar(ACH_ORCL));
    add_xpts = 0;
    if (u_pay == minor_cost) {
        outrumor(1, 0);
        if (!u.uevent.minor_oracle)
            add_xpts = (u_pay / (u.uevent.major_oracle | 0 ? 25 : 10)) | 0;
        u.uevent.minor_oracle = 1;
    } else {
        let cheapskate = schar((u_pay < major_cost));
        outoracle(cheapskate, (1));
        if (!cheapskate && !u.uevent.major_oracle)
            add_xpts = (u_pay / (u.uevent.minor_oracle | 0 ? 25 : 10)) | 0;
        u.uevent.major_oracle = 1;
        exercise(A_WIS, schar((!cheapskate)));
    }
    if (add_xpts) {
        more_experienced(add_xpts, (u_pay / 50) | 0);
        newexplevel();
    }
    return 1;
}

/** C ref: rumors.c:770 — @param {CPtr} filename */
function couldnt_open_file(filename) {
    let save_something = program_state.something_worth_saving;
    if (!iflags.debug_fuzzer)
        program_state.something_worth_saving = 0;
    impossible(__sl58, filename);
    program_state.something_worth_saving = save_something;
}

/** C ref: rumors.c:791 — @param {CPtr} word @returns {CInt} */
export function CapitalMon(word) {
    let nam;
    let i;
    let wln;
    let nln;
    if (!word || !cptr.ld1s(word) || cptr.ld1s(word) == lowc(cptr.ld1s(word)))
        return (0);
    if (!CapMons)
        init_CapMons();
    (__builtin_expect(BigInt((!(CapMons !== null))), 0n) ? __assert_rtn(__sl59, __sl60, 803, __sl61) : void 0);
    wln = Number(BigInt.asUintN(32, cptr.strlen(word)));
    for (i = 0; i < (CapMonSiz - 1) >>> 0; ++i) {
        nam = cptr.ldPtr(cptr.add(CapMons, i));
        nln = Number(BigInt.asUintN(32, cptr.strlen(nam)));
        if (wln < nln)
            continue;
        if (!cptr.strncmp(nam, word, BigInt(nln >>> 0)) && (!cptr.ld1s(cptr.add(word, nln)) || cptr.ld1s(cptr.add(word, nln)) == 32 || cptr.ld1s(cptr.add(word, nln)) == 39))
            return (1);
    }
    return (0);
}

/** C ref: rumors.c:829 */
function init_CapMons() {
    let pass;
    let bogonfile = fopen(__sl21, __sl1);
    if (CapMons)
        free_CapMons();
    for (pass = 1; pass <= 2; ++pass) {
        let mptr;
        let nam;
        let mndx;
        let mgend;
        CapMonstCnt = (CapBogonCnt = 0);
        for (mndx = LOW_PM >>> 0; mndx < NUMMONS >>> 0; ++mndx) {
            mptr = cptr.add(cptr.decay(mons), mndx);
            if ((cptr.ldU16(cptr.add(mptr, 34)) & 4096) != 0 && !the_unique_pm(mptr))
                continue;
            for (mgend = MALE >>> 0; mgend < NUM_MGENDERS >>> 0; ++mgend) {
                nam = cptr.ldPtr(cptr.add(mptr, mgend, 8));
                if (nam && cptr.ld1s(nam) != lowc(cptr.ld1s(nam))) {
                    if (pass == 2)
                        cptr.stPtr(cptr.add(CapMons, CapMonstCnt), nam);
                    ++CapMonstCnt;
                }
            }
        }
        if (bogonfile) {
            let hline = new Uint8Array(256);
            let xbuf = new Uint8Array(256);
            let endp;
            let startp;
            let code;
            void fseek(bogonfile, 0n, 0);
            void fgets(cptr.decay(hline), 256, bogonfile);
            while (fgets(cptr.decay(hline), 256, bogonfile)) {
                if ((endp = cptr.strchr(cptr.decay(hline), 10)) !== null)
                    cptr.st1(endp, 0);
                void xcrypt(cptr.decay(hline), cptr.decay(xbuf));
                unpadline(cptr.decay(xbuf));
                if (!cptr.ld1s(cptr.add(cptr.decay(xbuf), 0)) || !cptr.strchr(cptr.decay(bogon_codes), cptr.ld1s(cptr.add(cptr.decay(xbuf), 0))))
                    code = 0, startp = cptr.add(cptr.decay(xbuf), 0);
                else
                    code = cptr.ld1s(cptr.add(cptr.decay(xbuf), 0)), startp = cptr.add(cptr.decay(xbuf), 1);
                if (cptr.ld1s(startp) != lowc(cptr.ld1s(startp)) && !bogon_is_pname(code)) {
                    if (pass == 2)
                        cptr.stPtr(cptr.add(CapMons, (CapMonstCnt + CapBogonCnt) >>> 0), dupstr(startp));
                    ++CapBogonCnt;
                }
            }
        }
        if (pass == 1) {
            CapMonSiz = (((CapMonstCnt + CapBogonCnt) >>> 0) + 1) >>> 0;
            CapMons = alloc(Number(BigInt.asUintN(32, (BigInt(CapMonSiz >>> 0) * 8n))));
        } else {
            cptr.stPtr(cptr.add(CapMons, (CapMonSiz - 1) >>> 0), null);
            if (bogonfile)
                void fclose(bogonfile), bogonfile = null;
        }
    }
    if (flags.debug && debugcore(__sl62, (0))) {
        let buf = new Uint8Array(256);
        let i;
        let tmpwin = (windowprocs.win_create_nhwindow)(5);
        (windowprocs.win_putstr)(tmpwin, 0, __sl63);
        for (i = 0; i < (CapMonSiz - 1) >>> 0; ++i) {
            void cptr.sprintf(cptr.decay(buf), __sl64, cptr.ldPtr(cptr.add(CapMons, i)));
            (windowprocs.win_putstr)(tmpwin, 0, cptr.decay(buf));
        }
        (windowprocs.win_display_nhwindow)(tmpwin, (1));
        (windowprocs.win_destroy_nhwindow)(tmpwin);
    }
    return;
}

/** C ref: rumors.c:939 */
export function free_CapMons() {
    if (CapMons) {
        let idx;
        for (idx = CapMonstCnt; idx < (CapMonSiz - 1) >>> 0; ++idx)
            cptr.free(cptr.ldPtr(cptr.add(CapMons, idx)));
        cptr.free(CapMons), CapMons = null;
    }
    CapMonSiz = 0;
}
