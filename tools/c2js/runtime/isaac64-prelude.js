// ---- isaac64-prelude.js (marker only) ----
// isaac64.c is NEVER transpiled: the frozen js/isaac64.js is canonical
// (the judge overlays it on every scoring run; see tools/c2js/DESIGN.md
// "Frozen-module bindings"). build.mjs --all skips files that have a
// runtime prelude, which is the intended effect of this file existing.
// ---- end runtime prelude ----
